numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
rank_bm25>=0.2.2
pytest>=7.4

# optional — only needed if you set ANTHROPIC_API_KEY and want generated
# answers instead of the extractive fallback
anthropic>=0.40
